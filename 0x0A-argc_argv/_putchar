#include <stdio.h>
		
#include "main.h"
		

		
/**
		
 * _putchar – prints to stdout
		
 * @c:  this is what is to be printed
		
 *
		
 * Return: On success 1.
		
 * On error, -1 is returned and error is shown to us
		
 */
		
int _putchar(char c)
		
{
		
return (write(1, &c, 1));
		
}

